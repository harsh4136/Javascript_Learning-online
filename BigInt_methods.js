let bigint = BigInt(121);

let a = bigint.toLocaleString();      // for converting the variable from bigint to string .
console.log(typeof a)    //  output : string 

let c = bigint.toString();         // This also for converting the variable into string .
console.log(typeof c)         // output : string

console.log(bigint.valueOf())    // for finding the value inside the variable.
