let symbol = Symbol('harsh');

console.log(symbol.description);       // used for watching the value inside the variable.

let string = symbol.toString();      // used for converting the symbol into string 
console.log(typeof string)          // output : string 

console.log(symbol.valueOf());    // used for watching the value stored inside the variable .

