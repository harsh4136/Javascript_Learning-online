const car = ["BMW", "Lamborghini", "Ferrari"];
const cars = new Array("Saab", "Volvo", "BMW");  /* There is no need to use new Array().
                For simplicity, readability and execution speed, use the array literal method. */
const food = [];
food[0] = "Pizza" ;
food[1] = "Burger" ;
food[2] = "French fries" ;

console.log(car)    // accessing the whole array.
console.log(car[1]);     // accessing the array given an index number.

console.log(car['2'])    // accessing the array in different way.

 
